apiVersion: v1
kind: Service
metadata:
  name: {{ include "simplereact-frontend.fullname" . }}
  labels:
    {{- include "simplereact-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simplereact-frontend.selectorLabels" . | nindent 4 }}
